spring.application.name=backend
server.port: 8081

# Azure Blob Storage - placeholder (replace later with real values)
azure.storage.account-name=your-account-name
azure.storage.account-key=your-account-key
azure.storage.container-name=document-uploads

